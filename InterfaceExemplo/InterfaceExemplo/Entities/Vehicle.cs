using System;

namespace InterfaceExemplo.Entities
{
    class Vehicle
    {
        public String Model { get; set; }

        public Vehicle(string model)
        {
            Model = model;
        }
    }
}
